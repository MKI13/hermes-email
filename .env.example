# Version 0.13.0 requires no credentials and performs no automatic secret lookup.
# Future providers may explicitly resolve HERMES_EMAIL_USERNAME and HERMES_EMAIL_PASSWORD.
# Keep all values in the user-managed environment, never in this repository.
